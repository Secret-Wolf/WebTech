const express = require("express");
const app = express();

app.use

app.set("view engine", "ejs");
//views was ich angeben will, zweites views wie der Ordner heißt
//erstes ist das setting, das zweite der Wert
app.set("views", "views");

// Hier Code ergaenzen!

app.listen(8910);